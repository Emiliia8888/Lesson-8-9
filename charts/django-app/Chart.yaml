apiVersion: v2

name: django-app

description: Django application Helm chart

type: application

version: 0.1.0

appVersion: "1.0"

dependencies:
  - name: postgresql
    version: "15.5.17"
    repository: "https://charts.bitnami.com/bitnami"
